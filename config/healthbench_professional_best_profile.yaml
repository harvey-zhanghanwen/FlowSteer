# Current project pointer. The completion runner has no implicit default and
# still requires the executable config path explicitly.

schema_version: "flowsteer.agentgraph.current-best-profile-pointer.v1"
dataset_key: "healthbench_professional"
selected_profile_path: "config/healthbench_professional_best_profile_v1.yaml"
selected_condition_id: "healthbench_professional_official_v1"
next_run_config_path: "config/evaluation_healthbench_professional_official_v1.yaml"
runner_requires_explicit_config_argument: true
automatic_global_default_resolver: false
